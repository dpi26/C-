#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;   // Read three integers from standard input
    cout << a + b + c;    // Print their sum
    return 0;
}
